第5天：MyBatis-Plus 集成与基础表设计**
需学习的技术

1. MyBatis-Plus 核心注解（`@TableName`、`@TableId`、`@TableField`）**

2. MyBatis-Plus 内置 CRUD 方法（`insert`、`selectById` 等）
3. 数据库表设计：对话历史表、知识库表、工单表

核心任务
1. 集成 MyBatis-Plus，配置分页插件
2. 设计并创建 `chatrecord`、`knowledge`、`workorder` 三张核心表

要写的类/配置
`UserMapper`（接口，继承 BaseMapper<User>）**
`MybatisPlusConfig`（配置类，方法：`MybatisPlusInterceptor mybatisPlusInterceptor()`
`ChatRecord`（实体类，字段：id、userId、userMessage、aiAnswer、createTime）
`Knowledge`（实体类，字段：id、title、content、keyword、createTime）
WorkOrder`（实体类，字段：id、userId、type、level、content、status、createTime）

