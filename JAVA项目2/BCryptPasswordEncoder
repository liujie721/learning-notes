1.BCryptPasswordEncoder：它的实现类，自动加盐、最安全(加密工具类)
作用：
注册：密码加密
登录：密码比对
2.
@Configuration
public class SecurityConfig {

    // 交给 Spring 管理，随时可以 @Autowired 注入使用
    @Bean
    public PasswordEncoder bCryptPasswordEncoder() {
        return new BCryptPasswordEncoder();//创建 BCrypt 加密器,自动加盐、不可逆、防破解
    }
}
