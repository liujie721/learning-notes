1.总体任务完成
  JDK 21、Maven 3.8+、MySQL 9.6、IDE（IntelliJ IDEA）
  创建 Spring Boot 3.5.x 项目，引入 Spring Web、Spring AI、MySQL Driver 等依赖
  要写的类/配置：
    AiWorkOrderApplication（主启动类，含 `main` 方法）
    application.yml（配置端口、数据库连接占位符、Spring AI API Key 占位符）
2.学习的内容
  Maven:依赖管理+项目构建工具。可自动下载依赖。打包运行编译项目
  pom.xml:Maven里面的主要文件。在里面写项目信息，项目名称，配置等。写依赖，让其自动下载，让项目正常运行。构建配置。
  AiWorkOrderApplication：spring boot 的启动路口。启动整个 Spring Boot 项目启动 Tomcat 服务器加载所有配置、依赖、接口
  SpringApplication.run(AiWorkorderServiceApplication.class, args)：启动 Spring Boot 项目！
  resources:项目的「配置 / 资源存放处」
  Spring boot的学习
  Spring Ai的学习
