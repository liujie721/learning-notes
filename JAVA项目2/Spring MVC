前后端分离 = 前端做页面，后端做接口，完全分开开发、分开部署。
1.Spring MVC==Spring +MVC（分层思想）:它是 Spring 框架的一部分，专门用来做 Web 层开发（处理浏览器请求、接收参数、调用业务、返回结果）。
2.Spring MVC 是一个基于 Java 的 Web 开发框架，用来接收 HTTP 请求、处理请求、返回响应。
3.使用核心：
浏览器发请求（URL）
Spring MVC 找到对应方法（Controller）
方法处理 → 返回结果（页面 / JSON）
全程不用写 Servlet、不用配置 web.xml，全靠注解。
4.常用注解
@Controller: 标记MVC，处理HTTP请求，交给Spring容器管理
@RestController：@Controller + @ResponseBody 组合注解，返回 JSON / 字符串，不跳转页面（前后端分离专用）。
@RequestMapping:通用请求映射，绑定 URL 与处理方法，可指定请求方法、参数、请求头。
@GetMapping：GET 请求（查询数据）
@PostMapping：POST 请求（新增数据）
@PutMapping：PUT 请求（修改数据）
@DeleteMapping：DELETE 请求（删除数据）
@RequestParam:接收URL 查询参数（?key=value）或表单参数。
@PathVariable:接收URL 路径参数（/user/1001），RESTful 风格专用。
@RequestBody：接收JSON 格式请求体，自动封装为 Java 对象（前后端分离必备）。
@RequestHeader：接收请求头中的参数（如 token、User-Agent）。
@CookieValue：获取 Cookie 中的值
@ModelAttribute：接收表单参数，自动封装为对象；方法上标注：全局给视图添加公共数据。
@CrossOrigin：解决跨域请求问题（前后端分离必备）。
 @ResponseBody：将方法返回值直接写入响应体，不解析为视图（返回 JSON / 字符串）。
 @ResponseStatus：指定响应的 HTTP 状态码（如 201 创建成功、404 未找到）
@ComponentScan扫描指定包下的 Spring 组件（@Controller、@Service 等）。
 @ExceptionHandler：全局 / 局部异常处理，捕获控制器方法抛出的异常。
@ControllerAdvice：全局异常处理 + 全局数据绑定 + 全局预处理，配合 @ExceptionHandler 使用。
5.JSON 就是一种前后端通用的数据格式 **，专门用来在浏览器和服务器之间传递数据。前后端交流的语言
URL：URL = 网址 = 网络上资源的地址
GET = 去服务器 “拿数据”，只查询、不改数据。
POST = 提交数据
PUT = 修改、更新数据（完整覆盖）
DELETE = 删除数据
