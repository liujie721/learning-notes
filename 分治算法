1.概念：把大问题分割成小问题，把小问题分别解决，最终解决大问题。
2.分治算法的核心步骤
分治算法的执行流程可总结为 3 个关键步骤（缺一不可）：
分解（Divide）：将原问题拆分成若干个规模更小、独立的子问题（子问题与原问题类型相同）；
解决（Conquer）：如果子问题足够小（达到「基线条件 / 终止条件」），直接求解；否则递归拆分并解决子问题；
合并（Combine）：将子问题的解合并，得到原问题的解。
直观比喻
比如你要排序一个有 1000 个元素的数组：
分解：把数组拆成两个 500 元素的子数组；
解决：递归拆分直到子数组只有 1 个元素（此时天然有序）；
合并：把两个有序的子数组合并成一个有序数组（归并排序的核心）。
3.常见应用：
归并排序、快速排序（排序类）；
二分查找（分解为「找左半 / 右半」）；
大数乘法（Karatsuba 算法）；
矩阵乘法（Strassen 算法）；
逆序对统计（归并排序扩展）。
4.框架：
#include <iostream>
#include <vector>
#include <functional>

// 分治算法通用模板
template <typename T>
T divideConquer(
    T problem,  // 待解决的问题（如数组、区间、数值等）
    // 辅助函数1：判断问题是否足够小（基线条件）
    std::function<bool(const T&)> isSmallEnough,
    // 辅助函数2：解决足够小的问题（基线条件的具体实现）
    std::function<T(const T&)> solveSmallProblem,
    // 辅助函数3：拆分原问题为子问题
    std::function<std::vector<T>(const T&)> splitProblem,
    // 辅助函数4：合并子问题的解为原问题的解
    std::function<T(const std::vector<T>&)> mergeSolutions
) {
    // 1. 基线条件：问题足够小，直接求解
    if (isSmallEnough(problem)) {
        return solveSmallProblem(problem);
    }

    // 2. 分解：拆分原问题为子问题
    std::vector<T> subProblems = splitProblem(problem);

    // 3. 解决：递归处理每个子问题
    std::vector<T> subSolutions;
    for (const auto& subProblem : subProblems) {
        subSolutions.push_back(
            divideConquer(subProblem, isSmallEnough, solveSmallProblem, splitProblem, mergeSolutions)
        );
    }

    // 4. 合并：合并子问题的解
    return mergeSolutions(subSolutions);
}
