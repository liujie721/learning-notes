一基础
1.核心概念：
SwingUtilities.invokeLater(() -> { // GUI 代码 });Swing代码在这个里面，可以防止程序卡死
（1）
顶层容器：
JFrame:最常用的窗口（带标题栏、关闭按钮、最小化 / 最大化），是桌面应用的主窗口。
JFrame frame=new JFrame("")
方法：
关闭窗口，终止整个程序：JFrame.EXIT_ON_CLOSE
关闭窗口，只隐藏窗口：JFrame.HIDE_ON_CLOSE
关闭窗口 → 销毁窗口:JFrame.DISPOSE_ON_CLOSE
关闭窗口 → 无任何反应:JFrame.DO_NOTHING_ON_CLOSE
设置宽度高度：frame.setSize()
显示窗口：frame.setVisible()
窗口居中：frame.etLocationRelativeTo(null)
设置修改窗口标题：frame.setSize()

JDialog：对话框（如提示框、确认框），依赖于 JFrame。
JApplet：用于浏览器的小程序（几乎不用）。
（2）中间容器：用于组织组件，JPanel(面板)，JScrollPane(滚动面板)
（3）基本组件：按钮，文本框，标签。如JButton,JTextFiel,JLable
 (4)事件处理：Swing 是「事件驱动」的，比如点击按钮触发 ActionEvent，需要通过「监听器」处理。
核心事件类型：
ActionEvent：按钮点击、菜单选择、回车触发（最常用）。
ItemEvent：复选框 / 单选框 / 下拉框的选择状态变化。
KeyEvent/MouseEvent：键盘按键、鼠标点击 / 拖拽（如快捷键、自定义组件交互）。
监听器写法：
匿名内部类（快速实现简单逻辑）。
独立监听器类（复杂逻辑，便于复用和维护）。
事件适配器（如 MouseAdapter，避免实现接口的所有方法）。
2.常见的Swing组件
组件类	                功能	             简单用法示例
JLabel	        显示文本 / 图片	      new JLabel("用户名：");
JTextField	    单行文本输入	          JTextField tf = new JTextField(20); // 20 是列宽
JPasswordField	密码输入框	            new JPasswordField(20);
JTextArea	      多行文本输入 / 显示	  JTextArea ta = new JTextArea(5, 20); // 5 行 20 列
JButton       	按钮	
JCheckBox	      复选框	                new JCheckBox("记住密码");
JRadioButton	  单选按钮	              需配合 ButtonGroup 实现互斥
JComboBox	      下拉选择框	            JComboBox<String> cb = new JComboBox<>(new String[]{"Java", "Python", "C++"});
3.布局管理器
FlowLayout：流式布局（组件按添加顺序一行排，满了换行），默认 JPanel 的布局。
BorderLayout：边界布局（将容器分为东、南、西、北、中 5 个区域），默认 JFrame 的布局。
GridLayout：网格布局（将容器分成等大的行列表格），适合登录框、表单。
BoxLayout：盒子布局（垂直 / 水平排列），可精准控制组件间距。
4.
