1.File方法
（1）构造方法：（关联文件/目录的路径）File file=new File("路径")
（2）获取文件/目录的路径，名称的信息：file.getAbsolutePath();//获取绝对路径
（3）判断文件 / 目录的存在性、类型：
    文件/目录是否存在：file.exists()
    判断是否是文件：file.isFile();
(4)文件/目录的创建与删除：
  创建：File file = new File("D:/test.txt");
  删除：file.delete();
(5)获取文件大小：file.length()
2.CSV文件：以逗号为分隔的文本文件
3.构造方法注入实例：构造方法注入就是 “把类需要的依赖（如 DAO），从外部通过构造方法传给类，而非类自己创建”；
核心作用：
解耦：业务层与数据访问层分离，符合依赖倒置原则，易扩展；
安全：保证依赖初始化完整，避免空指针；
易测：支持模拟依赖，方便单元测试。

依赖：A要完成一件事情就一定要靠B。A依赖B
4.日志：记录程序运行过程中关键的信息。
使用方法：
5.public static final定义常量
