汇编语言：汇编给0/1起英语名，助写符，让人看懂，能写底层，速度快，一行汇编==一行机器码。
第一章绪论
1.8086汇编语言源程序允许使用2，8，10，16进制。并将其他进制化为2进制。
2.原码，反码，补码。（算法，转换）。字符编码ASCII码
第二章基本知识：
1.寄存器：存放当前正在用的数据，地址，计算结果。（跑的快，临时变量）
2寄存器组：
（1）通用寄存器（硬件，在cpu中）：
数据寄存器（只展现16位）：AX 累加器：加减运算
                        BX 基址：专门用来当地址（指向数组，数据）
                        CX 计数器：循环次数
                        DX 累加器的扩展：帮助进行乘除
间接寻址寄存器：BX
              BP 基指针：访问堆栈中的数据
              SI 源变址
              DI  目的变址
指针寄存器：SP（管栈顶），BP；
段寄存器：是 x86/x86-64 架构 CPU 中用于实现分段内存管理的专用寄存器，核心作用是指定当前访问的内存段，配合偏移地址形成完整的内存访问地址。
  偏移地址：在段里面，距离段开头的距离，就叫偏移地址
  DS：数据段  存放全局 / 静态数据的默认段
  ES：附加段  字符串操作的目标段
  SS：堆栈段 存放函数栈帧、局部变量
  CS：代码段 存放当前执行指令的段
指令指针IP
标志寄存器：
  状态标志：CF（进位，错位）
          PF（奇偶标志）
          AF（辅助进位标志）
          ZF（零标志）
          SF（符号标志）
          OF（溢出标志）
  控制标志（不重要）
3.存储器：存放程序和数据（CPU不可直接使用，要读到寄存器）
  内存：快，掉电消失
  外存：慢，掉电不消失
基本单位：位（0，1）
          1字节=10位
          1个字=2字节
最小单位：字节
每个字节等于一个地址
汇编语言中【】表示地址：BX，SI，DI，BP
8086是小段：存放低字节低地址
逻辑地址：段地址，偏移地址
物理地址：真正的地址
