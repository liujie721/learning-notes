1.索引：
（1）定义：帮助MySQL高效获取数据的有序数据结构（相当于目录）
（2）作用：加速查询，排序，分组。保证数据唯一性
（3）索引类型：
普通索引：只加速查询。像字典拼音目录，只管快速查
唯一索引：值唯一。像身份证号，不能重复
主键索引：特殊的唯一索引。像书的唯一编号，每本书只有一个
联合索引：多列组合成一个索引。像 “先按班级，再按姓名” 找学生
全文索引：用于文本搜索
（4）底层结构：B+树
（5）优缺点：/
优点：查询速度快
确点：增删改变慢
（6） 哪些字段适合建索引：高频查询、排序 / 分组、关联字段，且筛选性强、读多写少；低筛选性、频繁变更字段不建。
2.事务与索：
事务定义：把sql语句看作一个整体。要么全部成功，要么全部失败回滚，绝对不允许只成功一半。
索定义：索引就是数据库表的 “目录”。
    （1）事务 ACID（必考4个字母）
    - A 原子性：要么全成功，要么全失败，不可分割。
    - C 一致性：执行前后数据整体状态不变。
    - I 隔离性：事务之间互不干扰。
    - D 持久性：一旦提交，永久生效。
    （2）事务四大隔离级别（从上到下越来越严）
    1. 读未提交（Read Uncommitted）
    2. 读已提交（Read Committed）
    3. 可重复读（Repeatable Read）
    4. 串行化（Serializable）
    MySQL InnoDB 默认：可重复读（RR）
    （3）三种问题（脏读、不可重复读、幻读）
    1. 脏读
    - 读到了别的事务没提交的数据。
    - 别人回滚了，你读到的就是“脏数据”。
    2. 不可重复读
    - 同一个事务内，两次读取同一行结果不一样。
    - 原因：别的事务update 这行并提交了。
    3. 幻读
    - 同一个事务内，两次查询数量不一样，像出现幻觉。
    - 原因：别的事务insert / delete 并提交了。
    （4）隔离级别解决了哪些问题？
    - 读未提交：啥都没解决
    - 读已提交（RC）：解决脏读
    - 可重复读（RR）：解决脏读、不可重复读
    - 串行化：全部解决（性能极低）
    MySQL 在 RR 级别通过 间隙锁 很大程度解决了幻读。
    五、InnoDB 锁（面试必问）
    1. 表锁
    - 锁整张表
    - 并发差，简单
    - 不用索引时，行锁会升级成表锁
    2. 行锁
    - 锁某一行
    - 并发高，InnoDB 默认
    - 必须走索引，否则锁全表
    3. 间隙锁（Gap Lock）
    - 锁一段范围
    - 防止幻读
    - RR 级别才有
    4. 临键锁（Next-Key Lock）
    - 行锁 + 间隙锁 合在一起
    - InnoDB 默认行锁算法
3.数据库优化：（面试加分）
开启慢查询，找到慢 SQL，用 explain 分析
建立合适索引，避免索引失效
不使用select *，只查需要字段
减少大事务、长事务
大表进行分库分表
合理设计表结构，避免冗余
4.三大范式：
第一范式：字段不可分，原子性
第二范式：完全依赖主键
第三范式：不传递依赖
5.alter:表的结构（列、字段、表本身）→ 对表的框架修改
添加：ALTER TABLE 表名 ADD 字段名 类型;
删除：ALTER TABLE 表名 DROP 字段名;
修改字段类型 / 约束：ALTER TABLE 表名 MODIFY 字段名 新类型;
改字段名字：ALTER TABLE 表名 CHANGE 旧字段名 新字段名 类型;
修改表名
ALTER TABLE 旧表名 RENAME TO 新表名;

添加主键
ALTER TABLE 表名 ADD PRIMARY KEY(字段名);

添加索引
ALTER TABLE 表名 ADD INDEX(字段名);
6.
