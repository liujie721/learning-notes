1.创建数据库 create database 数据库名

2.创建时指定字符集：
create database 数据库名
default character set utf8mb4
collate utf8mb4_unicode_ci;
    ENGINE=InnoDB:数据库存储引擎
    default charset=utf8mb4:数据库字符集
3.使用数据库：use 数据库名

4.删除数据库：drop 数据库名

5.查看已有数据库：show databases;

6.建表：
use 数据库名；
create table 表名（
字段1 类型 约束，
字段2 类型 约束
）
字段：这一列的名字
类型：存储类型  int  double(m,n);m（总位数）,n（小数点几位）；varchar(长度，可变)，char(长度，固定) ；data，datatime。
约束： primary key (主键) ，not null(非空)，unique（唯一），default(默认)，auto_increment(自动增长)。

7.主键：一张表里，用来唯一标识每一行数据的字段，就叫 主键。

8.增：insert
增加一个学生
    insert into student(name, age, sex)
    values('张三', 18, '男');
删：delete
-- 删除 id=1 的学生
    delete from student
    where id=1;
改：update
-- 把 id=1 的学生年龄改成 20
    update student
    set age=20
    where id=1;
查：select
    select name, age from student;
    select * from student where age=18;
    select * from student;

9.关键字：
into：表示把数据插到哪个表    insert into student  → 把数据插到 student 表里
values:后面写要添加的数据     values('张三',18,'男')
from:表示从哪个表查、删、改   select * from student;  从student表查
                            delete from student;    从student表删
where:加条件（查谁、改谁、删谁） where id=1  → 只对 id=1 这一行有效
set:修改时用，把字段改成新值   set age=20  → 把年龄改成20
on:在… 基础上；根据…
select:保存的
update:
delete:
and:并且
or:或者
like:模糊查询

limit：分页
-- 方式1：仅限制行数（取前N行）
SELECT ... LIMIT N;
-- 方式2：指定起始位置（偏移量）+ 行数（通用分页写法）
SELECT ... LIMIT 偏移量, 行数;
-- 偏移量计算：(页码-1) * 每页行数（比如第2页、每页10条：偏移量=10）

max,min:
-- 求单列最大值
SELECT MAX(列名) [AS 别名] FROM 表名 [WHERE 筛选条件];
-- 求单列最小值
SELECT MIN(列名) [AS 别名] FROM 表名 [WHERE 筛选条件];

avg:求平均值
SELECT AVG(列名) [AS 别名] FROM 表名 [WHERE 筛选条件] [GROUP BY 分组列];

UNION 是用于合并多个 SELECT 查询结果集的关键字，它能将多个查询的结果纵向拼接成一个结果集返回。
union
union all
select 列1, 列2 FROM 表1
union [all]
select 列1, 列2 FROM 表2;
特性	     UNION	             UNION ALL
重复行处理	自动去重（删除重复）	保留所有重复行
性能	    较慢（需比对去重）	较快（无去重操作）
         适用场景	需无重复结果	 允许重复 / 追求效率

order by:对查询结果按指定列排序，实现有序展示
降序必须加 DESC

distinct:去重
between...and...:在… 之间

    UPDATE Salary
    SET sex = 
    CASE 
        WHEN sex = 'm' THEN 'f'
        WHEN sex = 'f' THEN 'm'
    END;
 case：SQL 中的条件判断关键字，类似编程语言中的 if-else 逻辑，用于根据不同条件返回不同的值。
when 条件 then 结果
end；
CASE...WHEN...THEN...END 是 SQL 中的条件分支结构，用于根据不同条件设置不同的值，替代了编程语言中的 if-else。

SELECT 
    teacher_id, 
    COUNT(DISTINCT subject_id) AS cnt  -- 去重后统计科目数量，别名设为cnt
FROM 
    Teacher
GROUP BY 
    teacher_id;  -- 按教师ID分组
count():统计行数
as：给列 / 表达式起别名
group by:作用是按指定列分组,包含所有非聚合字段

sum:聚合
SUM(列名/表达式)  -- 对指定列的所有非NULL值求和
SUM(DISTINCT 列名)  -- 先对列值去重，再求和（较少用）

length():计算字符串的字节数（受字符集影响，比如 UTF-8 中一个中文占 3 字节）；
char_length():计算字符串的字符数（无论字符集，一个中文 / 字母都算 1 个字符）。

统计重复数据 = GROUP BY + HAVING
HAVING = 对「分组后的数据」做过滤
WHERE：过滤单行
GROUP BY：分组
HAVING：过滤分组（搭配 COUNT 用）
UNION ALL：把三个查询的结果合并成一张表

① 升序（从小到大 / A→Z）默认就是这个
ORDER BY Email ASC
ASC 可以省略不写
② 降序（从大到小 / Z→A）
ORDER BY Email DESC

REGEXP高级版模糊查询
能匹配：
开头 ^
结尾 $
包含
数字
字母
复杂格式

逗号（,）：是「分隔符」，用于同一语法结构下的多个并列元素（字段、分组字段、插入值等），CASE WHEN 子句内无逗号；
分号（;）：是「终止符」，仅放在整条 SQL 语句的最后，语句中间（如函数、CASE END 内）绝对不能加；


10.多表查询：从多张表中查询数据
（1）内连接：
select * 
from student
inner join class 
on student.cid = class.cid;
（2）左连接：左边全部显示
select * 
from student
left join class 
on student.cid = class.cid;

join：连接
on：按哪个字段连接（必须写！）
inner join：只显示两边都有的
left join：左边表全部显示
